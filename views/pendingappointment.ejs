<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet" />
    <style>
        /* Basic reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }

        /* Table styling */
        table {
            width: 60vw;
            border-collapse: collapse;
            font-size: 18px;
            text-align: center;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            overflow: auto;
            margin-left: 20vw;
            max-height: 80vh;
        }

        thead tr {
            background-color: #009879;
            color: #ffffff;
            text-align: left;
        }

        th,
        td {
            padding: 12px 15px;
            border: 1px solid #dddddd;
        }

        tbody tr {
            border-bottom: 1px solid #dddddd;
        }

        tbody tr:nth-of-type(even) {
            background-color: #f3f3f3;
        }

        tbody tr:last-of-type {
            border-bottom: 2px solid #009879;
        }

        .styled-button {
            /* background: linear-gradient(135deg, #6e7dff, #4c5baf); */
            background-color: #049648;
            ;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            font-weight: bold;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        /* Hover effect */
        .styled-button:hover {
            background: linear-gradient(135deg, #049648);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
            transform: translateY(-3px);
        }

        /* Focus effect */
        .styled-button:focus {
            outline: none;
            box-shadow: 0 0 8px rgba(110, 125, 255, 0.5);
        }

        h1 {
            /* color: #007BFF; */
            color: #049648;
            /* Blue color */
            font-size: 48px;
            /* Large font size */
            font-family: 'Arial', sans-serif;
            /* Font family */
            text-align: center;
            /* Center the text */
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
            /* Add a shadow */
            padding: 20px;
            /* Add some space around the heading */
            margin: 20px 0;
            /* Spacing between heading and other elements */
            letter-spacing: 2px;
            /* Spacing between letters */
            border-bottom: 2px solid #049648;
            /* Blue underline effect */
            transition: color 0.3s ease-in-out;
            /* Transition effect for hover */
        }

        /* Styling the small green button */
        .small-green-button {
            background-color: #049648;
            /* Green background */
            color: white;
            /* White text */
            border: none;
            /* Remove border */
            padding: 8px 16px;
            /* Small padding */
            font-size: 14px;
            /* Smaller font size */
            font-family: 'Arial', sans-serif;
            /* Clean font */
            border-radius: 5px;
            /* Rounded corners */
            cursor: pointer;
            /* Pointer cursor */
            transition: all 0.3s ease;
            /* Smooth transition for hover */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            /* Subtle shadow */
        }

        /* Hover effect */
        .small-green-button:hover {
            background-color: #037a3a;
            /* Slightly darker green on hover */
            transform: scale(1.05);
            /* Slightly increase size on hover */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            /* Enhance shadow on hover */
        }

        /* Focus effect */
        .small-green-button:focus {
            outline: none;
            /* Remove default outline */
            box-shadow: 0 0 8px rgba(4, 150, 72, 0.5);
            /* Add a glow when focused */
        }
    </style>
</head>

<body>
    <div>
        <div style="width: 100vw;text-align: center; margin-bottom: 3rem;">
            <h1>
                <%= pappointments.length %> Approved Appointments
            </h1>
        </div>
        <div style="width: 100vw;text-align: center;">
            <table>
                <tr>
                    <th>
                        Pet
                    </th>
                    <th>
                        Date
                    </th>
                    <th>
                        Symptomps
                    </th>
                </tr>
                <% for(appointment of pappointments){ %>
                    <tr>
                        <td>
                            Pet - <%= appointment.petname %>
                        </td>
                        <td>
                            Date - <%= appointment.date %>
                        </td>
                        <td>
                            Symptomps - <%= appointment.symptomps %>
                        </td>
                    </tr>
                    <% } %>
            </table>
        </div>
        <div style="width: 100vw;text-align: center; margin-top: 2rem;">
            <form style="border: none;" class="nav-link" action="http://localhost:8080/" method="get">
                <button type="submit" class="styled-button">
                    Back to Home
                </button>
            </form>
        </div>
    </div>
</body>

</html>